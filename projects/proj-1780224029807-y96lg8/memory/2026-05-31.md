# 2026-05-31 项目进展

## 已完成

- 按 project-init 流程完成需求对齐。
- 用户确认清理范围为整个工作区 `E:\workspace\LingTour`。
- 用户确认执行方式为“先列清单”，不会直接删除。
- 用户确认测试文件策略为“仅删临时测试”，保留正式测试资产。
- 已更新 `project.md`，记录目标、原则、里程碑和任务拆解。
- 已批量创建 7 个项目任务，覆盖扫描、清单、确认、执行、报告、验证和记忆更新。
- 已扫描工作区并生成 `outputs/cleanup-candidates.md`。
- 已将“扫描工作区并识别清理候选”和“生成待清理清单”两个任务标记完成，产出为 `outputs/cleanup-candidates.md`。
- 已按用户确认清理低风险项，生成 `outputs/cleanup-execution.json`。
- 已将管理员后台主体移动到根目录 `admin-frontend/`。
- 已同步更新后台迁移相关配置和部署文档：`docker-compose.prod.yml`、`ecosystem.config.js`、`admin-frontend/Dockerfile`、`BT-DEPLOY-PATHS.md`、`docs/DEPLOYMENT.md`、`docs/docker-deployment.md`。
- 已生成清理与迁移报告：`outputs/cleanup-result.md`。
- 已完成清理后基础验证，并生成 `outputs/verification-result.md`。
- 已更新项目里程碑，M5 已标记完成。
- 已继续清理历史项目目录 `projects/proj-1778970788262-5mcldi`，删除其中绝大多数项目记录、输出物和临时测试脚本。
- 已在 `admin-frontend`、`api`、`site` 中重新安装依赖。
- 已完成构建复验：`api` 与 `site` 构建通过。
- 已修复 `admin-frontend` 迁移后的共享 JSON 路径问题，并完成后台构建复验。

## 重要发现

- 工作区存在多个可重新生成目录：`node_modules`、`.next`、`dist`、`coverage` 等。
- 存在多处日志文件：根目录和 `api/`、`site/`、历史项目输出目录。
- 存在临时测试/手工验证文件：如 `screenshot-test.txt`、`test-save.txt`、历史项目中的 `test-api.js`、`test-proxy.js`。
- 未将正式测试目录或 `*.spec.*` / `*.test.*` 文件作为直接删除目标。
- 候选清单建议优先删除低风险可重建目录、日志文件和明确临时测试文件；`tmp`、`.codex-logs`、历史测试脚本/报告等仍建议确认后再处理。
- 清理执行中有部分 `node_modules` 或运行时目录因进程/权限占用删除失败，包括 `api/node_modules`、`site/node_modules`、旧后台 `node_modules` 等。
- 管理员后台迁移后，旧路径 `projects/proj-1778970788262-5mcldi/admin-frontend` 仍残留被占用/忽略内容，主要为 `.tmp` 和 `node_modules`。
- 根仓库 Git 状态中后台迁移主要显示为 rename；部分迁移文件因原本已有本地修改显示为 `RM`。
- `admin-frontend/.git` 已保留后台独立仓库 remote：`https://github.com/qiuy-collab/LingTour-admin-frontend.git`。
- 验证阶段确认 `admin-frontend/src`、`api/src`、`site/src/app` 等关键源码目录仍存在，未发现源码主体被误删。
- 本轮旧项目整目录删除时，Windows 句柄占用了两个残留文件：`.tmp/ssh-key` 与 `node_modules/@rolldown/binding-win32-x64-msvc/rolldown-binding.win32-x64-msvc.node`，导致旧目录暂未彻底删除。
- `admin-frontend` 最初构建失败已定位为迁移后路径问题：`src/constants/guangdongRegions.ts` 中 `../../../../../shared/route-regions.json` 实际解析到了 `E:\shared\route-regions.json`，而不是工作区内的 `E:\workspace\LingTour\shared\route-regions.json`。
- 现已同步修复 `admin-frontend/src/constants/guangdongRegions.ts` 与 `admin-frontend/tsconfig.app.json` 中的 shared 路径；`admin-frontend`、`api`、`site` 三个项目均已成功构建。
- `admin-frontend` 当前构建仍提示 Vite chunk size warning，但不影响本轮验证通过。

## 待办

- 关闭相关 Node/Vite/编辑器/浏览器进程后，再尝试清理剩余被占用目录。
- 彻底删除 `projects/proj-1778970788262-5mcldi` 最后残留的 `.tmp` 与 `node_modules` 占用文件。
- 如需进一步优化后台产物体积，可后续单独处理 `admin-frontend` 的 Vite chunk size 警告。

## 交流记录

- 用户询问是否可以把后台主体移到根目录，以及这样 git 是否会变化。
- 已说明：目录移动会体现在 Git 工作区中；如果内容不变，Git 通常会在 diff/status 中识别为 rename/move；建议用 `git mv` 并先确保工作区干净。
- 用户随后要求先根据删除清单清理无需确认删除的文件，再将管理员后台文件夹移动到根目录，并处理相关 Git 配置。
- 用户进一步要求：若旧项目内容都已迁移，则旧项目目录也一起删除，只保留当前项目。
- 用户选择先执行第 3 项：重装依赖并做 `admin-frontend / api / site` 的构建复验。
- 用户随后选择继续执行第 1 项：直接修复 `admin-frontend` 的共享 JSON 路径并重新 build。

## 本次执行记录

- 任务：修复 `admin-frontend` 迁移后的共享 JSON 路径问题，并重新执行后台构建。
- 状态：完成。
- 修复动作：
  - 将 `admin-frontend/src/constants/guangdongRegions.ts` 中的导入从 `../../../../../shared/route-regions.json` 改为 `../../../shared/route-regions.json`
  - 将 `admin-frontend/tsconfig.app.json` 中的 include 从 `../../shared/**/*.json` 改为 `../shared/**/*.json`
- 验证动作：执行 `npm run build --prefix E:\workspace\LingTour\admin-frontend`
- 结果：后台构建成功，Vite 已正常产出 `dist/`。
- 补充说明：构建过程中仍有 chunk size warning，但不影响本轮迁移修复验证结论。
- 产出：已更新 `outputs/verification-result.md`、`project.md`、`memory/long-term.md`、`memory/2026-05-31.md`。
