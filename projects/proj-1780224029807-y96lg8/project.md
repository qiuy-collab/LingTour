# 项目计划：初始化项目并清理本地无用/测试文件

## 项目目标

在保证安全、不误删有效源码和正式测试资产的前提下，完成当前长期项目的初始化，并对整个工作区 `E:\workspace\LingTour` 进行无用文件、临时测试文件的盘点与清理。

## 用户已确认的关键偏好

- 清理范围：整个工作区 `E:\workspace\LingTour`
- 执行方式：先列清单，确认后再删除
- 测试文件处理：仅删除临时测试文件，保留正式单元测试、集成测试、端到端测试等质量保障文件
- 迁移需求：将管理员后台主体从历史项目目录移动到工作区根目录，并同步相关 Git/部署配置

## 清理原则

1. **先审计，后删除**：先生成候选清单和风险分级，不直接删除。
2. **保留正式测试资产**：保留 `test/spec/__tests__/e2e` 等正式测试体系中的有效文件。
3. **只清理明显临时内容**：优先识别 `tmp/temp/cache/log/demo/ad-hoc/scratch` 等临时文件、构建缓存、空文件、重复产物。
4. **避免破坏项目依赖**：不删除锁文件、配置文件、源码入口、数据库迁移、正式文档等关键文件。
5. **可追溯**：清理前输出清单，清理后输出结果报告。

## 里程碑

- ✅ M1：项目初始化与需求对齐
- ✅ M2：扫描整个工作区并生成待清理清单
- ✅ M3：用户确认清理范围和候选文件（本轮用户确认先清理无需二次确认项）
- ✅ M4：执行清理并记录删除结果
- ✅ M5：运行基础验证，确认清理未破坏项目
- ✅ M6：沉淀清理规则和长期记忆（初始化阶段）

## 任务拆解

1. 扫描工作区文件结构，识别缓存、构建产物、临时文件、临时测试文件和重复/空文件。
2. 生成 `outputs/cleanup-candidates.md`，按类别、风险等级、建议动作列出候选项。
3. 等待用户确认候选清单，必要时调整保留/删除规则。
4. 执行已确认的删除动作，并生成 `outputs/cleanup-result.md`。
5. 运行基础验证（如目录完整性检查、必要时运行项目已有测试/构建命令）。
6. 更新项目文档、长期记忆和当日进展记录。

## 预期产出

- `project.md`：项目计划与里程碑
- `outputs/cleanup-candidates.md`：待清理候选清单
- `outputs/cleanup-execution.json`：清理执行明细
- `outputs/cleanup-result.md`：清理与后台迁移结果报告
- `outputs/verification-result.md`：清理后验证结果报告
- `memory/long-term.md`：长期偏好与清理原则
- `memory/2026-05-31.md`：当日进展记录

## 当前状态

- 已清理低风险目录、日志、明确临时测试文件。
- 已将管理员后台主体移动到根目录 `admin-frontend/`。
- 已同步更新 Docker、PM2、部署文档中的后台路径引用。
- 已完成基础验证：关键源码目录仍在，未发现源码主体被误删。
- 已在 `admin-frontend`、`api`、`site` 重新安装依赖，并完成复验。
- `admin-frontend`、`api` 与 `site` 构建均已通过；其中 `admin-frontend` 的迁移后共享 JSON 路径问题已修复。
- 已停止多个指向旧历史项目路径的 Node/Vite 进程，并继续清理 `projects/proj-1778970788262-5mcldi`。
- 旧历史项目目录的文件内容已基本清空；当前仅剩被 Windows 句柄占用的空目录 `projects/proj-1778970788262-5mcldi/admin-frontend/`，导致父目录暂时无法物理删除。重启或关闭持有句柄的程序后可最终删除。
