# 2026-05-21 工作日志

## P0 审计修复（已完成）

基于 `outputs/admin-audit-report.md` 审计报告，完成全部 6 项 P0 级修复：

### 后端变更（api/）
- `src/modules/orders/entities/order.entity.ts` — 新增 `updatedAt`, `trackingNo`, `refundReason` 列
- `src/modules/orders/dto/update-order-status.dto.ts` — 新建，校验 6 种状态枚举
- `src/modules/orders/dto/ship-order.dto.ts` — 新建，可选 trackingNo
- `src/modules/orders/dto/refund-order.dto.ts` — 新建，可选 reason
- `src/modules/orders/orders.service.ts` — 新增 `updateStatusAdmin`, `shipOrder`, `refundOrder`（含状态流转校验）
- `src/modules/orders/orders.controller.ts` — 新增 3 个 PATCH 端点

### 前端变更（admin-frontend/src/）
- `types/interpreting.ts` — InterpreterFormData.name/language/city → I18nObject；ServiceModeFormData.price → I18nObject；BookingStatus 扩展为 8 种
- `api/bookings.ts` — 删除 normalizeBooking 中的状态合并逻辑
- `views/interpreting/InterpreterEdit.vue` — 改用 I18nInput + toI18n
- `views/interpreting/ServiceModeEdit.vue` — price 改用 I18nInput + toI18n
- `views/interpreting/BookingsList.vue` — 状态筛选扩展、操作按钮条件更新
- `views/routes/RouteEdit.vue` — 添加 rules + formRef.validate + citySlugs 改 el-select
- `views/cities/CityEdit.vue` — 添加 rules + formRef.validate
- `views/shop/ProductEdit.vue` — 添加 rules + formRef.validate
- `views/events/EventEdit.vue` — 添加 rules + formRef.validate
- `views/home/HomeConfig.vue` — featuredRoutes 改 el-select + 加载路线列表
- `mock/interpreting.ts` — 'pending' → 'new' 适配新类型

### 验证结果
- ✅ NestJS `npm run build` 零错误
- ✅ `vue-tsc --noEmit` 零错误
- ✅ `vite build` 零错误，78 个 chunk 全部正常产出

### 注意事项
- 后端新增了 3 个数据库列（updated_at, tracking_no, refund_reason），需要运行数据库迁移
- Booking 状态从前端 4 种扩展为 8 种，后端 DTO 中的有效值为 `new|read|contacted|confirmed|cancelled`
