# Canton Porcelain Tea Cup — `canton-porcelain-cup`

46 SGD · collection `null` · cover + 3 gallery images

Production record fetched read-only 2026-09-19. Replace the field below; leave
every field not listed untouched.

## `story` — replace

Before:

> A clean white porcelain cup chosen as a calm companion for Canton morning tea
> and afternoon table conversation.

After:

> A plain white porcelain cup for the repeated refills of Canton morning tea — the
> kind that stays on the table all afternoon rather than being put away between
> rounds.

Why: the published line tells the buyer how to feel about the cup (*"a calm
companion"*) instead of what it is for. The replacement keeps the two facts
already in it — white porcelain, morning tea through the afternoon — and states
the use.

## Unchanged (verify before writing)

- `product.name` = `Canton Porcelain Tea Cup`
- `product.tag` = `Tea table object` — see the register mismatch in the README;
  left as published.
- `materialNotes` = `Porcelain`
- `price` = `46`, `currency` = `SGD`
- `image` = `/uploads/shop/canton-porcelain-cup-cover-70d252a9713a.jpg`
- `gallery` (3 records, all preserved byte-for-byte):
  - `/uploads/shop/canton-porcelain-cup-gallery-1-f02373ae2ae6.jpg`
  - `/uploads/shop/canton-porcelain-cup-gallery-2-117598bdf6ed.jpg`
  - `/uploads/shop/canton-porcelain-cup-gallery-3-e12450015c83.jpg`
- `primaryMedia` = cover, `galleryMedia` (3 records), `collection` = `null`